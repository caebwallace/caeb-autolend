import { Logger } from '@helpers/logger/logger';

describe('@helpers/logger', () => {
    describe('Logger should exists', () => {
        it('should exists', () => {
            expect(Logger).toBeDefined();
        });
    });
});
